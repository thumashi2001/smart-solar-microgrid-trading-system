<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".SmartSolarApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartSolarMicrogrid">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.operator.OperatorHomeActivity"
            android:exported="false" />

        <activity
            android:name=".ui.prosumer.ProsumerQrEntryActivity"
            android:exported="false" />

        <!--
            Navigation entry (Prosumer / deep link from booking flow):
            startActivity(Intent(this, QrDisplayActivity::class.java).apply {
                putExtra(QrDisplayActivity.EXTRA_RESERVATION_ID, reservationId)
            })
        -->
        <activity
            android:name=".ui.qr.QrDisplayActivity"
            android:exported="false" />

        <activity
            android:name=".ui.scanner.QrScannerActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.verification.VerificationActivity"
            android:exported="false" />

        <activity
            android:name=".ui.transfer.TransferResultActivity"
            android:exported="false" />

        <activity
            android:name=".ui.map.StationsMapActivity"
            android:exported="false" />
    </application>
</manifest>
